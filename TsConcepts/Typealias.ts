//Type aliases let you name a type and reuse it. The syntax is: type AliasName = <your type here>
/*
 with type alias — clean and reusable
type Student = { name: string; age: number };

let user1: Student = { name: "Tom", age: 20 };
let user2: Student = { name: "Sara", age: 22 };

function getUser(): Student { ... }
function saveUser(user: Student) { ... }
 */